# CI placeholder values — safe to commit, contains no real secrets.
# Copy to local.properties and replace with real keys for local development.
# local.properties is gitignored and must never be committed.
gemini.api.key=PLACEHOLDER
